
// tạo hàm
function sum(x, y) {
  return x + y
}

// function multiply(x, y){
//   return x * y
// }

// tạo 1 biến const và gán nó vào 1 cái hàm
const multiply = (x, y) => {
  return x * y
}

// one - line function
const substract = (x, y) => x - y 

export {
  sum,
  multiply,
  substract
}